@{
    ViewData["Title"] = "About";
}
<h2>@ViewData["Title"]</h2>
<h3>@ViewData["Message"]</h3>

<p>These are the cities we will visit on this trip:</p>


@{
    foreach(var city in (IEnumerable<string>)ViewData["Cities"])
    {
        <p>@city</p>
    }
}